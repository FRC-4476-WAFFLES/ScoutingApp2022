import { FunctionComponent } from "react";
import { 
    SafeAreaView,
    Text,
} from "react-native";

import { colors } from "../components/colors";

const Home: FunctionComponent = () => {
    return (
        <SafeAreaView>
            
        </SafeAreaView>
    );
}

export default Home;